# Base URL of the backend API (include the /api path prefix).
# Local dev default shown below; set to the deployed Render backend's
# URL (still with /api) for production/staging Vercel environments.
NEXT_PUBLIC_API_URL=http://localhost:5000/api
